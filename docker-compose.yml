# INF-002 — Postgres + Redis locales.
# Uso: docker compose up -d
# Este entorno de agente puede no tener Docker; en ese caso usar Postgres del sistema
# con las mismas credenciales de .env.example.

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-turnero}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-turnero}
      POSTGRES_DB: ${POSTGRES_DB:-turnero}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - turnero_pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-turnero} -d ${POSTGRES_DB:-turnero}"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  turnero_pg:
