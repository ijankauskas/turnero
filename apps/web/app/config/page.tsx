'use client';

import PlaceholderPage from '../../components/placeholder-page';

export default function ConfigPage() {
  return (
    <PlaceholderPage
      title="Configuración"
      note="ABM de empresa, sucursales y usuarios. PATCH /company ya está para Admin."
    />
  );
}
