'use client';

import PlaceholderPage from '../../components/placeholder-page';

export default function ClientesPage() {
  return (
    <PlaceholderPage
      title="Clientes"
      note="El listado entra en UI-007. La API recorta por tenant y rol."
    />
  );
}
